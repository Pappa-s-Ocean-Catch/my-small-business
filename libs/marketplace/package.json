{
  "name": "@my-small-business/marketplace",
  "version": "1.0.0",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "test": "rm -rf dist-test && tsc -p tsconfig.test.json && node --test dist-test/test/**/*.test.js",
    "type-check": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.3"
  }
}
